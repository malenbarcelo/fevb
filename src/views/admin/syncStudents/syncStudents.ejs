<%- include('../../partials/head') %>
<%- include('../../partials/header') %>

<div class="fs-19 fw-b mt-25 mb-35 ta-c">CURSOS SINCRÓNICOS - INSCRIPCIONES</div>


<div class="flex-c rg-20">
    <% branches.forEach(branch => { %>
        <div class="btn-std w-300 pad-8-0 m-a" id="<%= 'branch_' + branch.id %>"><%= 'Traer datos ' + branch.branch %></div>
    <% }) %>
</div>

<div class="flex-c rg-20 mt-50">
    <% branches.forEach(branch => { %>
        <div class="btn-std w-300 pad-8-0 m-a" id="<%= 'update_' + branch.id %>"><%= 'Actualizar Asistencia y Pagos ' + branch.branch %></div>
    <% }) %>
</div>

<!-- loader -->
<div class="loader ld-std" id="loader">
    <div class="ld-content top-40p"></div>
</div>

<!--- popups --->
<%- include('../../partials/resultsPopups') %>

<!----------SCRIPTS---------->
<script type="module" src="/js/admin/syncStudents/syncStudents.js?v=<%= Date.now() %>"></script>
